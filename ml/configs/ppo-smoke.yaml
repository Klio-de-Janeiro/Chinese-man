ppo:
  base_checkpoint: artifacts/ml-smoke/imitation/best.pt
  output_dir: artifacts/ml-smoke/ppo
  seed: 2026
  updates: 1
  episodes_per_update: 8
  player_counts: [2, 3]
  batch_size: 128
  ppo_epochs: 1
  learning_rate: 0.0003
  weight_decay: 0.0001
  gamma: 1.0
  gae_lambda: 0.95
  clip_ratio: 0.2
  entropy_coefficient: 0.01
  value_coefficient: 0.5
  max_grad_norm: 0.5
  heuristic_probability: 0.2
  historical_probability: 0.3
  checkpoint_every: 1
  max_historical_models: 2
